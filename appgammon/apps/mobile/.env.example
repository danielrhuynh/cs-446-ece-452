# Optional override for backend API base URL.
# By default, the app auto-detects the Expo host and uses http://<expo-host>:3000.
# Set this only when you intentionally want a different backend host.
EXPO_PUBLIC_API_URL=
